{% extends "PidevSiteBundle:Default:layout.html.twig" %}
{%block content%}
    <h1>Facture list</h1>

    <table class="records_list">
        <thead>
            <tr>
                
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('facture_show', { 'id': entity.id }) }}"></a></td>
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('facture_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('facture_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('facture_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
