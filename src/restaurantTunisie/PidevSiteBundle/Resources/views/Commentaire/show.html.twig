{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Commentaire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ entity.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date }}</td>
            </tr>
            <tr>
                <th>Idrestaurant</th>
                <td>{{ entity.idRestaurant }}</td>
            </tr>
            <tr>
                <th>Idclient</th>
                <td>{{ entity.idClient }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('commentaire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('commentaire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
